# Copy to .env in this folder (optional) and:  set -a; source .env; set -a
# Or export these in your shell before running setup_conversions.py

# Path to the service account JSON key downloaded from GCP (GA4 Admin API)
# GA4_SERVICE_ACCOUNT_JSON=/path/to/tripoint-admin-xxxxx.json

# Google Ads API (only needed for Step 2 — conversion actions)
# GOOGLE_ADS_DEVELOPER_TOKEN=
